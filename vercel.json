{
    "buildCommand": "echo 'No build needed for API'",
    "outputDirectory": ".",
    "installCommand": "yarn install",
    "framework": null,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        }
    ]
}
